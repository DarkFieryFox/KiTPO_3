package git.group
package View

object main extends App {

  var consol = new Test
  var flag: Boolean = false
  if (flag == false) {
    flag = consol.toBuilder("Integer")
    flag = consol.toBuilder("GPS")
  }
  consol.run()
}